extends ../layout/index

block content
    div.py-10
        a(href="/auth/login")
            h1(class="hover:cursor-pointer").text-4xl.my-10.font-extrabold.text-center Bienes
                span.font-normal Raices
        h2.text-center.text-2xl.font-extrabold #{pagina}

    div(class= error ? 'bg-red-100 border-red-500 text-red-700' : 'bg-green-100 border-green-500 text-green-700').border-l-4.p-3.text-sm.my-3.rounded-md.max-w-md.mx-auto.text-center
        p.text-center.font-bold #{mensaje}

        if !error
            a(href="/auth/login" class="text-gray-500 text-center font-bold mt-10 block uppercase hover:underline") Inicia Sesión